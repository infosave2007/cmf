English: [README.md](README.md) · 中文: [README.zh.md](README.zh.md)

# CMF — Cortiq Model Format

**Один файл, который несёт веса, токенизатор и шаблон чата, проверяет собственную целостность и работает без ML-фреймворка.**

[![CI](https://github.com/infosave2007/cmf/actions/workflows/ci.yml/badge.svg)](https://github.com/infosave2007/cmf/actions/workflows/ci.yml)
[![crates.io](https://img.shields.io/crates/v/cortiq-core.svg)](https://crates.io/crates/cortiq-core)
[![downloads](https://img.shields.io/crates/d/cortiq-cli.svg)](https://crates.io/crates/cortiq-cli)
[![docs.rs](https://img.shields.io/docsrs/cortiq-core)](https://docs.rs/cortiq-core)
[![License: Apache-2.0](https://img.shields.io/badge/license-Apache--2.0-blue.svg)](LICENSE)

Ни torch, ни BLAS, ни ONNX, ни установки CUDA, ни C++-тулчейна. Небольшое ядро
на Rust: CPU везде, GPU через нативный Metal и wgpu (Vulkan / DX12). Веса
отображаются в память и читаются на месте. Один флаг превращает внимание модели
в оператор постоянной памяти — без дообучения и не меняя ни одного веса.

## Попробуйте

```sh
cargo install cortiq-cli          # или готовый бинарник со страницы релизов

cortiq convert --model Qwen/Qwen3-0.6B --quant q8 --output qwen.cmf
cortiq run qwen.cmf --prompt "Столица Франции?" --greedy --no-think
```

```console
Ready: qwen3 | Task: general | Sparsity: 0%
Столица Франции — **Париж**.
[10 tokens, 40.1 tok/s, finish: stop]
```

Уже есть GGUF? `cortiq import-gguf <файл-или-репозиторий> --output model.cmf`.

Без установки: [конвертировать модель](https://huggingface.co/spaces/infosave/cmf-converter) ·
[отрисовать картинку](https://huggingface.co/spaces/infosave/cmf-imagine) ·
[посмотреть ролики](https://huggingface.co/spaces/infosave/cmf-animate)

## Оценка против других форматов

Восемь критериев со своими весами, каждый формат оценён от 0 до 100. Полная
матрица и обоснование каждой ячейки: [docs/COMPARISON.ru.md](docs/COMPARISON.ru.md).

| | CMF | GGUF | safetensors | ONNX | PyTorch | GGML | TensorRT |
|---|---:|---:|---:|---:|---:|---:|---:|
| **Взвешенный итог /100** | **80** | **86** | 53 | 56 | 45 | 55 | 52 |
| *без веса «экосистема»* | **97** | 83 | 41 | 48 | 33 | 63 | 50 |

GGUF выигрывает по сумме — и заслуженно: 20 очков из 100 приходятся на
экосистему, и там у CMF 15 против 100. Один автор, первый публичный релиз в
июле 2026 года.

По свойствам самого контейнера CMF впереди, 97 против 83: хэш целостности на
каждый тензор, чего нет ни у одного другого однофайлового формата,
специалисты, живущие внутри файла под той же цепочкой хэшей, и лестница
квантования, доходящая до тернарного.

Обе цифры верны. Нужна модель, которая заработает сегодня вечером на железе,
которое кто-то уже проверил, — это GGUF. Нужен один проверяемый файл, несущий
N специалистов и доказывающий собственную целостность, — это сюда.

## Что внутри файла

Фиксированный 128-байтовый конверт, за ним секции, адресуемые только через
него, никогда по предполагаемому порядку:

| секция | что хранит |
|---|---|
| header JSON | архитектура, кванты по умолчанию, chat bundle, реестр навыков, происхождение |
| каталог тензоров | записи по 56 байт: имя, dtype, форма, смещение, размер, `hash64` |
| блоб весов | выровнен по страницам, отображается и читается на месте |
| навыки | маски задач и замещающие тензоры каждого навыка |
| токенизатор | дословный файл Hugging Face |

```sh
cortiq verify model.cmf     # конверт, секции, каждый хэш тензора
cortiq info   model.cmf     # архитектура, тензоры, квантование, навыки
cortiq sign   model.cmf     # отделённая Ed25519-подпись поверх SHA-256
```

`.cmf` либо валиден, либо `open()` падает громко — ловятся обрезание файла и
битовая гниль. Также несёт: MTP-головы, MoE-слои, смешанное глобальное и
скользящее внимание, двойной RoPE/YaRN, дописывание навыков и разбиение на N
самостоятельно валидных файлов.

**Вы ничем не связаны.** `python/cmf_reader.py` — полноценный ридер на ~300
строках stdlib + numpy, написанный по спецификации и не разделяющий ни строчки
кода с рантаймом на Rust:

```python
from cmf_reader import CmfReader
r = CmfReader("model.cmf")
w = r.tensor("model.layers.0.mlp.gate_proj.weight")   # np.ndarray, деквантованный
assert r.verify() == []                               # сходятся все хэши
```

Нормативная спецификация: [docs/CMF_V2_SPEC.ru.md](docs/CMF_V2_SPEC.ru.md).

## Квантование

Потензорное и смешиваемое — можно держать внимание в q8, а FFN опустить до q4 в
одном и том же файле.

| квант | бит/параметр | примечание |
|---|---|---|
| `f16` | 16 | без квантования |
| `q8` | 8 | масштаб на строку |
| `q8_2f` | 8 | масштаб на строку **и** на столбец — качество выше при том же размере |
| `q4` · `q4t` | 4.5 | блочный / чередующиеся тайлы |
| `q4tp` | **4.17** | `q4t` с предсказанными масштабами — на 7% меньше, +0.1% ошибки |
| `q2tp` | ~2 | смешанный профиль 2/4 для MoE |
| `vbit` | ~4.25 | переменные 3–8 бит |
| `q1t` | 2.25–3.5 | тернарный без обучения + разреженный слой выбросов ([док](docs/Q1T_PTQ.md)) |
| `q1` | 1.5 | для чекпоинтов, **обученных** бинарно (Bonsai / BitNet) |

`q4tp` в одну строку: тайл `q4t` тратит 16 бит на f16-масштаб для 32 весов — это
11% файла. Если сделать масштаб 5-битной ступенью геометрической лестницы на
строку, цена — **+0.1% относительной ошибки** на медианном разбросе внутри
строки. Существующие файлы переводятся на месте, чекпоинт не нужен:

```sh
cortiq requant model.cmf --output model-q4tp.cmf --quant q4tp
# KAT-Coder-V2.5: 12.65 → 11.80 ГБ (19254 тензора) за 2 минуты
```

## Внимание O(1)

`--o1` заменяет softmax-внимание слоя состоянием фиксированного размера:
несколько точных якорных ключей, точное недавнее окно и опорный эскиз всего
более старого — под одним общим знаменателем. **Веса не меняются**: флаг лишь
записывает подсказку в заголовок.

Qwen3.5-4B (сконвертированы 8 softmax-слоёв), Apple M4:

| контекст | `--o1 off` | `--o1 all` | декод |
|---:|---:|---:|---:|
| 543 | 141.0 МБ | **124.1 МБ** | 15.7 → 16.5 tok/s |
| 1055 | 174.5 МБ | **124.1 МБ** | 15.5 → 16.5 tok/s |
| 4127 | 380.3 МБ | **124.1 МБ** — в 3.1 раза меньше | 8.2 → 10.7 tok/s |

Постоянно на любой длине. KV, который он заменяет, растёт примерно на 64 КиБ на
токен, поэтому кривые пересекаются около 290 токенов: ниже `--o1` стоит
несколько мегабайт, выше — только экономит.

**Чего это стоит.** Перплексия растёт в **1.13 раза** на Qwen3.5-4B и в **1.30**
на Qwen3-0.6B (28 слоёв из 28) на отложенном wikitext. Чем больше в модели
softmax-внимания, тем дороже. Это ручка «память против качества», а не
бесплатный выигрыш — измерьте на своей модели:

```sh
cortiq ppl model.cmf --file wiki.txt --o1 all   # печатает точный базовый уровень рядом
cortiq run model.cmf --o1 all|deep12|off        # или решайте при загрузке
```

`cortiq fcd` возвращает часть цены ограниченным нативным проходом обучения —
без Python и без ML-фреймворка.

## Против llama.cpp

Qwen2.5-0.5B-Instruct, Apple M4, точное внимание с обеих сторон, чередующиеся
запуски из свежих процессов, каждая сторона на своём лучшем числе потоков.
`cortiq bench --core` соответствует контракту `llama-bench`.

| | `llama.cpp` (q8_0) | CMF (q8) | Δ |
|---|---|---|---|
| tg128, CPU, их лучший `-t 6` | 165.5 tok/s | 151–158 | −5% |
| tg128, CPU, их дефолт `-t 4` | 129.4 tok/s | 151–158 | **+18%** |
| tg128, их Metal `-ngl 99` | 150.9 tok/s | 151–158 (CPU) | **CPU у CMF ≥ их GPU** |
| pp512, CPU | 1168 tok/s | 1017–1051 | −12% |
| pp512, GPU | 3333–3396 | 2742–3215 | −5% лучшее против лучшего |
| PPL против собственного f16 | почти без потерь | +0.38% | сопоставимо |
| Размер файла | 644 МБ | **479 МБ** | **−26%** |

Воспроизвести: `cortiq bench --json --core`.

## Много специалистов, один хребет

Поставка N дообученных моделей обычно означает N полных копий. CMF держит один
хребет плюс по одному небольшому навыку: навык хранит только те тензоры,
которые замещает, рантайм читает их вместо хребтовых, а неиспользуемый навык
стоит **нуля оперативной памяти**. На диске `|хребет| + Σ|навыки|`, а не
`N × |модель|`.

На своей задаче навык снижает перплексию на **24.9%** относительно хребта, на
котором сидит (отложенные данные, [спец. §9](docs/CMF_V2_SPEC.ru.md)).

```sh
cortiq skill add ...                            # запечь из донорского чекпоинта
cortiq run model.cmf --prompt "SELECT ..." --skill sql
cortiq route model.cmf --prompt "..."           # или пусть выберет сам, `explain` покажет почему
```

Три реальных навыка из публичных дообучений, запечённые в один файл на 0.5B,
вместе с режимами отказа: [docs/SKILLS.ru.md](docs/SKILLS.ru.md).

**Специалисты из MoE.** Использование экспертов сильно зависит от задачи: код и
проза маршрутизируются в почти непересекающиеся наборы (Jaccard по top-64 равен
0.25). `cortiq moe-defrag` физически выбрасывает экспертов, которых задача
никогда не использует: кодер на 34.7B худеет с **19.6 до 12.7 ГБ (−35%)** при
+2.8% перплексии на коде, а на MacBook с 24 ГБ, где полная модель свопилась,
специалист помещается и декодирует **в 1.8 раза быстрее**. `cortiq moe-mask`
запекает то же ограничение переключаемой маской задачи — один файл,
`run --task coder`, токен в токен как физическая обрезка.
[docs/KAT_CODER.ru.md](docs/KAT_CODER.ru.md)

## Спекулятивный декод

Модель с MTP-головой черновичит ею и проверяет цепочку одним батчевым сабмитом.
**Включено по умолчанию для жадного декода файлов q4tp.** Монитор сравнивает
токены-за-раунд с обычным токеном и выключает спекуляцию после четырёх
проигранных раундов, повторяя попытку позже, — так промпт, который выигрывает,
выигрыш сохраняет, а тот, который нет, идёт на обычной скорости.

- Qwen3.8-27B q4tp, RTX 5090: **76 tok/s против обычных 48.5**, принято 90% черновиков
- Она же на M4 mini (24 ГБ): обычный декод 6.7, тело кода **12.2**; промпт на 447 токенов — **11.6 с** до первого токена
- `CMF_VERIFY_I8=0` делает поток бит в бит равным обычному пути, `CMF_GRAPH_SPEC=0` выключает спекуляцию

Этот декод **упирается в шину**: два процесса на одной карте дают в сумме
52.8 tok/s против 48.8 у одного, а матвек, лишённый всей арифметики, проходит
тот же токен с точностью до 4%. Рычаги — читать меньше байт или разложить
чтение на несколько токенов, а не ускорять ядро.
[docs/GPU_KERNEL_RECIPES.md](docs/GPU_KERNEL_RECIPES.md)

## Он ещё и рисует

Тот же контейнер, тот же бинарник, никакого Python на инференсе.

| `cortiq animate` — видео **и его звуковая дорожка** | `--first-frame` — продолжить с картинки |
|---|---|
| ![корги в поварском колпаке подбрасывает блин](docs/media/corgi.gif) | ![тот же ролик, продолженный со стоп-кадра](docs/media/keyframe.gif) |

Звук не подкладывается потом: он расшумляется в той же упакованной
последовательности, что и видео, по своему расписанию потока, — и приходит уже
синхронным.

- **`cortiq animate`** — MiniMax-H3 + Turbo LoRA. 512×288, 39 кадров, четыре шага, из одного файла на 23.9 ГБ (при 124.4 ГБ эталонного дерева). Одна RTX 5090: **60.2 с**.
- **`cortiq ltx-video`** — LTX-2.5, DiT на 21B с совместным аудиопотоком. Восемь шагов или `--two-stage` для детализации. [docs/LTX.md](docs/LTX.md)
- **`cortiq imagine`** — Lumina-Image 2.0, дерево diffusers на 19 ГБ в **3.2 ГБ** `.cmf`. M4: 256×256 за 30 шагов примерно за 37 с.

<img src="docs/media/fox-512.png" width="340" alt="рыжая лиса в снежном лесу">

## На телефоне

Тот же формат на Android и iOS: **[Cortiq: Local AI Models](https://play.google.com/store/apps/details?id=ai.cortiq.cmf_mobile)**
несёт этот же рантайм нативной библиотекой — общайтесь с `.cmf` прямо на
устройстве, конвертируйте репозиторий с Hugging Face в CMF на самом телефоне и
отдавайте загруженную модель в локальную сеть по тому же OpenAI-совместимому API.

- Что умеет — **[huggingface.co/spaces/infosave/cortiq-mobile](https://huggingface.co/spaces/infosave/cortiq-mobile)**
- Исходники — [github.com/infosave2007/cmfmobile](https://github.com/infosave2007/cmfmobile) (Apache-2.0)

В паре с `cortiq worker` на настольной машине телефон тянет модель больше
собственной памяти: MoE на 34.7B и **16.3 tok/s** на аппарате, где свободно 2 ГБ.

## Сервер

`cortiq serve` говорит на OpenAI API, так что существующие клиенты работают без
изменений.

```sh
cortiq serve model.cmf --port 8080     # плюс веб-панель на /
```

`/v1/chat/completions`, `/v1/completions`, `/v1/models`, `/healthz`, стриминг.
Трезво о границах: **запросы обрабатываются последовательно** и
**аутентификации нет** — это локальный сервер, а не многопользовательский шлюз.

## Команды

| команда | что делает |
|---|---|
| `convert --model <hf-репо\|каталог>` | чекпоинт HF → `.cmf` (нативный Rust) |
| `import-gguf <файл\|hf-репо>` | GGUF → `.cmf`, все распространённые кванты ggml |
| `run` · `serve` | чат / один вопрос; OpenAI-совместимый сервер |
| `info` · `verify` · `masks` · `diff` | осмотреть, проверить целостность, сравнить версии |
| `bench` · `ppl` | tok/s и память; перплексия с учительским форсированием |
| `requant` · `compact` | сменить квантование на месте; уплотнить контейнер |
| `skill add` · `list` · `route` · `explain` | запечь, перечислить и маршрутизировать специалистов |
| `moe-defrag` · `moe-mask` | физически выбросить неиспользуемых экспертов или сделать их переключаемыми |
| `fcd` | восстанавливающий тренер для моделей с `--o1` |
| `sign` | отделённая Ed25519-подпись |
| `imagine` · `animate` · `ltx-video` | картинка, видео со звуком, LTX-2.5 |
| `imagine-pack` · `animate-pack` · `ltx-pack` | упаковать эталонное дерево в один `.cmf` |
| `worker` · `peers` | отдавать слои другой машине; найти её в локальной сети |

`cortiq <команда> --help` описывает каждый флаг.

## GPU

```sh
CMF_GPU=1 cortiq run model.cmf
```

Бэкенд выбирается сам — Vulkan на Linux/Windows, DX12 как запасной, Metal на
macOS. **Включение GPU никогда не делает медленнее**: для каждого класса
операций движок на старте измеряет оба плеча и оставляет быстрое
(`CMF_GPU_PROBE=0` доверяет устройству безусловно).

И на Metal, и на дискретных картах весь токен исполняется одним графом с одним
считыванием обратно: скрытое состояние живёт на устройстве через все слои,
внимание считается там же, а маршрутизируемый MoE выполняет свой роутер, top-k
и всех выбранных экспертов в том же сабмите. Измерено:

| | |
|---|---|
| Bonsai-27B q1, RTX 4090 | **40 tok/s** (в 7.7 раза быстрее её CPU-пути), 38 на контексте 4K |
| KAT-Coder 34.7B-A3B, RTX 5090 | **32.8 tok/s** против 14.4 на её 32-ядерном хосте |
| Nanbeige4.2-3B, безвентиляторный MacBook Air M4 | **22.4 tok/s**, приём промпта 181 tok/s |

Вывод эквивалентен CPU-пути по распределению, но не бит в бит: суммирование с
плавающей точкой идёт в другом порядке, как при любом выносе на GPU.

**Больше одной GPU** — два флага под две разные задачи. `serve --gpus N` кладёт
полную реплику на каждую карту (2×RTX 5090, MoE на 34.7B: 115.3 tok/s на один
запрос, **218.5 суммарно** на два). `run --gpus N` режет стек слоёв между
картами для моделей крупнее одной карты — это покупает место, а не скорость, и
стоит несколько процентов на модели, которая и так помещается. `--peer` делает
тот же раскол по сети. [docs/MULTI_GPU.md](docs/MULTI_GPU.md)

## Запустится ли ваша модель?

Нативная конвертация: qwen2 · qwen3 · qwen3.5 (включая сплавленный qwen3_next) ·
llama · mistral · qwen-moe · gemma / gemma-2 / gemma-3 · gemma-4 плотные 12B/31B
и MoE 26B-A4B · gemma-3n E4B · phi-3 / phi-4 · дистилляты DeepSeek-R1 ·
DeepSeek-V2 MLA · Kimi Linear 48B-A3B · MiniCPM3 · чекпоинты в упаковке MXFP4.
Пока нет: специфика Kimi-K3, до публикации её кода моделирования.

Всё остальное — попробуйте `import-gguf`. Если он откажется, это баг, о котором
стоит сообщить.

## Установка и сборка

```sh
cargo install cortiq-cli                 # утилита командной строки
cargo add cortiq-core                    # или формат из вашего кода на Rust

cargo build --release --workspace                            # бэкенд wgpu включён по умолчанию
cargo build --release -p cortiq-cli --no-default-features    # только CPU
```

Готовые бинарники для Linux x86-64, macOS (Apple Silicon и Intel), Windows
(x86-64 и ARM64) и `aarch64-linux-android`, каждый с `.sha256`:
[последний релиз](https://github.com/infosave2007/cmf/releases/latest).

```
crates/cortiq-core     чтение формата: конверт, каталог, кванты, маски, mmap
crates/cortiq-engine   переносимый CPU/GPU-рантайм, токенизатор, чат, навыки
crates/cortiq-server   OpenAI-совместимая отдача по HTTP
crates/cortiq-cli      команда `cortiq`
python/                эталонный ридер — stdlib и numpy, больше ничего
docs/                  спецификация, сравнение, разборы моделей
```

## Состояние

**Формат — это устоявшаяся часть.** Он v2: читатели навигируют только через
конверт, неизвестные поля заголовка игнорируются, а ломающее изменение стоит
бита возможностей или смены версии — но никогда не молчаливого
переосмысления. Написанный сегодня `.cmf` останется читаемым, и контракт этого
— `cortiq verify`.

API крейтов ещё могут поменяться до 1.0. Первый публичный релиз — июль 2026,
один автор. Все изменения в [CHANGELOG.md](CHANGELOG.md).

Баги и пожелания: [заведите issue](https://github.com/infosave2007/cmf/issues).
Безопасность: **не** открывайте публичный issue — см. [SECURITY.md](SECURITY.md).
Модель, которая не конвертируется, — это баг-репорт, а не ошибка пользователя.

> **Счётчики скачиваний на Хабе.** Файлы `.cmf` пока не входят в реестр учёта
> загрузок, поэтому репозитории CMF показывают `0` при реальном трафике.
> Исправление на стороне Хаба ожидается:
> [huggingface.js#2354](https://github.com/huggingface/huggingface.js/pull/2354).

## Лицензия

**Apache-2.0** ([LICENSE](LICENSE)) — используйте, меняйте, поставляйте
коммерчески.

Программа практикует методы, заявленные в четырёх поданных патентных заявках США
([PATENTS.md](PATENTS.md)). Раздел 3 Apache-2.0 даёт вам бессрочную,
всемирную, безвозмездную лицензию на те притязания, которые неизбежно
затрагиваются этой программой в поставляемом виде: **запуск, форк и поставка
покрыты**, и грант отзывается только если вы подадите патентный иск против
проекта. Он ограничен этой Работой; для независимой реализации контейнера
напишите на urevich55@gmail.com — доступен грант для реализаторов.

Истоки замысла, с жёсткой границей между тем, что измерено, и тем, что остаётся
метафорой: [принципы VMF/NVG за CMF](VMF_principles_in_CMF.ru.md)
([English](VMF_principles_in_CMF.md) · [中文](VMF_principles_in_CMF.zh.md)).
