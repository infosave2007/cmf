# Cortiq Embryo — саморазвивающееся ядро на нашем формате

*Технология-черновик, 16 августа 2026. Собрана из наших патентов и
измерений (P1–P17, VMF-фаза, кампании Metal/5090). Всё, что «расчёт» —
помечено; всё остальное — измерено или уже работает в рантайме.*

## 0. Идея

Не «ещё одна модель на N миллиардов», а **зародыш**: маленькое ядро,
которое рождается на обычном Mac (M4, 24 ГБ), учится днём, спит ночью,
само растёт слоями и навыками, никогда ничего не забывает — потому что
рост у нас структурно append-only — и живёт в одном `.cmf`-файле, который
только прирастает. Формат, роутер, скилы, O(1)-внимание, спекуляция,
кванты — всё это уже есть в нашем рантайме; зародыш — первый организм,
спроектированный под них с рождения.

**Пять «вау»-сцен (все на одном M4):**
1. Зародыш 20 M параметров учится с нуля за ночь (≈12 ч на M4) и к утру
   говорит по-русски и по-английски, пишет простой код.
2. Скармливаешь ему свою документацию/переписку. Утром — новый навык
   (запись в файле), старые навыки **байт-в-байт** те же (проверяется
   sha256 записей). Файл вырос на мегабайты, а не пересобран.
3. Пока ты не спрашиваешь, зародыш «спит» — консолидируется в простое:
   похожие навыки слились, общее знание кристаллизовалось в ядро, маски
   сжались в генератор, веса переквантованы — файл стал **меньше и
   быстрее**, качество проверено гейтом, откат автоматический; первый же
   запрос мгновенно прерывает сон.
4. Контекст 128k на 24 ГБ при **постоянной** памяти: 7 из 8 слоёв —
   наш O(1)-кор, якоря — в режиме o1. Скорость не падает с глубиной.
5. Навык, выученный у тебя, — файл в мегабайт: посылаешь другу, его
   зародыш подхватывает (рой на общем ядре, P15).

## 1. Из чего собран (карта наших технологий)

| механизм | откуда | роль в зародыше | статус |
|---|---|---|---|
| Резонанс-роутер: выбор навыка по ошибке реконструкции входа в аффинном подпространстве навыка, без классификатора и без меток; OOD = min-ошибка > τ | P1 (подан), P3 | «кора» выбора режима: какой навык/эксперт/маска работает над входом; тот же сигнал — триггер роста | задеплоено (237 масок, `masks.rs`) |
| Замороженное ядро + маски Q/K и FFN (DTG-MA/FCD), append-only записи навыков в контейнере | P2 (подан), P15 (подан) | **нет катастрофического забывания структурно**: старые записи неизменны, новые — только добавляются | в рантайме CMF (skills, masks, overlays) |
| Самораширение: OOD-буфер → кластер → новая маска (crossover-seed двух ближайших навыков) → регистрация; генератор масок G(z); офлайн-консолидация | P3, P15-provisional | автоматическое появление навыков днём | trigger задеплоен; crossover/консолидация — прототип |
| Агентный контур: System 1 (маска) / System 2 (размышление), дистилляция удачных траекторий System 2 → маску System 1, эпизодическая память, валидатор | P4 | «сон» переводит найденные рассуждения в быстрые навыки | прототип |
| Кристаллизация консенсуса (≥80% масок согласны → впечатать в ядро без градиента), метакогнитивный монитор, любопытство (инверсия OOD), модель мира | P6 | ночная эволюция ядра под гейтом с откатом; монитор петель/неуверенности | описано, код 0 → зародыш = первая реализация |
| WTA-открытие навыков без меток (конкуренция реконструкции, separation loss, Парето-отбор) | P9 | ночью: из накопленных данных выделяются навыки сами | `dtgma_denoise_real.py`, denoising→+точность |
| Ретайпинг/морф архитектуры (перенос функционального ядра слоя в соседний, непрерывный морф под Lyapunov-гейтом) | P10, P11 | «сон» уменьшает и уплотняет сеть | живой движок 64→63, 18.2B GGUF |
| Двухполевой квант, q4tp/q2tp | P13, формат | ночной реквант под гейт ppl; эксперты в 2 бита | в рантайме |
| O(1)-внимание: конверсия и потоковые расширения | P16 (подан), P17 | якорные слои «o1-ready»; длинный контекст | в рантайме (`--o1`, Metal) |
| VMF-фаза / hybrid_k: фазовые признаки + постоянный decay + гейт κ; init горизонтов; численность чанк-скана | наши эксперименты 06–07.2026 | **основной кор зародыша** (O(1) на токен, помехоустойчивость) | оп в рантайме (`vmf_phase`) |
| Спекуляция на b-строчном verify-графе с replay-коммитом рекуррентного состояния; shortlist-голова | кампания 16.08 (0.5.82) | скорость декода ×1.7–2.5 | реализовано (Metal) |
| Exact-margin голова словаря | план fast-convert | голова читается на 5%, точно | математика готова |

Ключевое отличие от «просто continual learning»: у нас забывание невозможно
не потому, что мы «аккуратно учим», а потому что **старое физически не
переписывается** (append-only записи, byte-identical). Это свойство формата.

## 2. Жизненный цикл

```
 РОЖДЕНИЕ ──► НАГРУЗКА (служит + учится) ──► ПРОСТОЙ (сон) ──► РОСТ ──► НАГРУЗКА …
                     ▲                                        │
                     └──────────── РОЙ (обмен навыками) ◄─────┘
```

**Рождение.** Ядро 20 M параметров учится с нуля на M4 (§4). Ядро после
рождения замораживается («геном»); дальше меняются только записи вокруг
него и — редко, под гейтом — кристаллизованные параметры.

**Нагрузка («день»).** На каждый вход роутер (P1) считает резонанс с записями навыков →
выбирает маску/эксперт-группу; ответ идёт быстрым путём (System 1). Если
резонанс низкий (OOD) — вход в буфер + при уверенности ниже порога
включается System 2 (пошаговое рассуждение той же моделью). Метакогнитивный
монитор (P6) следит за петлями/осцилляцией роутера и переключает режимы.
Пользовательские данные копятся в буферах OOD по кластерам.

**Сон = простой.** Это не «ночь по расписанию» и не человеческий сон:
сон — любое время, когда к зародышу нет запросов (нет входов N минут,
GPU/CPU свободны, на ноутбуке — питание/температура в норме). Он
начинается сам, **прерывается мгновенно** первым же запросом (каждый шаг
атомарен и append-only, поэтому обрыв безопасен) и продолжается с того же
места, когда нагрузка уходит. Между запросами идут микро-шаги (секунды),
в длинном простое — полный цикл. Последовательность с гейтами и откатом
(§5): (1) из
OOD-кластеров и удачных System-2 траекторий выделяются навыки (P9-WTA,
P4-дистилляция) и учатся масками над замороженным ядром (минуты–часы на
M4); (2) похожие навыки сливаются, редкие — усыпляются; (3) консенсус
масок кристаллизуется в ядро (P6) — маски упрощаются; (4) генератор масок
G(z) переобучается — библиотека сжимается 10–50× (P3); (5) реквант
(P13/q4tp/q2tp) и морф/ретайпинг (P10/P11) — файл меньше, декод быстрее;
(6) якоря переопираются (reseal лендмарков, P17) — o1-режим свежий.
Каждый шаг: гейт качества (ppl на репрезентативном наборе + задачи навыков)
и откат к предыдущей записи; всё — новые записи, старые не трогаются.

**Рост.** Когда буфер OOD велик и маски перестают помогать (насыщение —
§4.3), зародыш растёт: новые эксперты (копия ближайшего + шум), новый
слой (тождественная инициализация: выходная проекция = 0 → выход сети
не меняется в момент вставки; место — по стоимости размещения P10),
новая запись навыка. Обучаются только новые части + маски; старые байты
неизменны. Рост — тоже append-only.

**Рой.** Запись навыка (маска/replacement-тензоры + дескриптор роутера) —
самостоятельный файл-мегабайт; другой зародыш с тем же геномом принимает
его без обучения (P15). Геном версионируется; кристаллизации ядра
публикуются как «поколения генома».

## 3. Архитектура зародыша

### 3.1 Слой
```
x ─► RMSNorm ─► [маска Q/K навыка] ─► МИКШЕР ─► +res ─► RMSNorm ─► [маска FFN] ─► ЭКСПЕРТЫ (top-1 + общий) ─► +res
```
- **МИКШЕР** = наш кор hybrid_k (7 слоёв из 8): φ(θ)=[cos θ, sin θ], nphase=32,
  постоянный decay по лог-сетке горизонтов [8, 2L], гейт записи κ=σ(W_κx+2);
  состояние 8 голов × 64 × 128 на слой. Каждый 8-й слой — **якорь**: GQA
  softmax 8q/2kv, hd 128, режим o1-ready (sink 4 + окно 128 + 16 лендмарков,
  reseal каждые 256 позиций внутри обучения).
- **ЭКСПЕРТЫ**: FFN как 4 эксперта inter 768 (top-1) + общий 768. Это
  «слоты роста»: новые эксперты добавляются записями. Роутинг экспертов
  берёт сигнал резонанс-роутера (router-is-coarse-MoE: эксперт = крупный
  навык), не отдельный gate.
- **Маски** (Q/K и FFN, DTG-MA) — слот навыка; в ядре по умолчанию единичные.

### 3.2 Размеры и рост

| | Embryo-0 (рождение) | Embryo-1 (после ~10 ростов) | Embryo-2 (взрослый на M4) |
|---|---|---|---|
| слоёв | 8 (7 hk + 1 якорь) | 16 (14 + 2) | 24–32 |
| hidden | 384 | 384 | 512 (рост ширины — морф P11) |
| экспертов/слой | 4 + общий | 12 + общий | 32 + общий |
| параметры всего / активных | 56 M / 35 M (12.6 M — эмбеддинг) | ~90 M / 30 M | ~400 M / 60 M |
| словарь | 32k, иерархический (128×256), tied | | |
| MTP-головы | 2 | 2 | 3 |
| файл q4tp | ~30 МБ | ~60 МБ | ~250 МБ |
| декод M4 (расчёт по нашим ядрам) | >400 tok/s | >300 | ~200 (+спекуляция) |

Обучение всех тиров — на M4 (§4). Взрослый Embryo-2 при желании
«переезжает» на большой бюджет тем же механизмом роста (ядро замораживается,
растут записи) — без потери накопленного.

### 3.3 Роутер (P1)
Дескриптор навыка — аффинное подпространство скрытых состояний слоя L/2
(среднее + k=16 главных направлений); резонанс = ошибка реконструкции
входа. Выбор навыка = argmin; OOD = min-ошибка > τ (95-й перцентиль
in-distribution). Иерархия дескрипторов держит O(log N). Тот же дескриптор
пишется в запись навыка (P15) — обмен навыками переносит и роутинг.

### 3.4 Голова, MTP, спекуляция
Иерархическая голова (128 кластеров × 256) с exact-margin декодом; две
MTP-головы (t+2, t+3) — под наш verify-граф; hybrid_k совместим с
replay-коммитом (как GDN сегодня). Спекуляция у 20-M модели даёт мало
(она и так >400 tok/s), у Embryo-2 — ×1.5–2.

## 4. Обучение и рост на M4 24 ГБ

### 4.1 Бюджеты (расчёт: 6·N_active·T; **измерено 16.08:** наш f32-GEMM на
simdgroup-матрицах (`cortiq-embryo bench`, Metal, без MPS) держит **2.7–2.8
TFLOPS** на M4 (10 ядер GPU) на всех трёх ориентациях обучения — y=x·Wᵀ,
dx=dy·W, dW=dyᵀ·x — и на всех формах зародыша, включая голову 32k; ниже
время считаем от ~2 TFLOPS устойчивых с учётом не-GEMM части шага)

| этап | параметры | токены | FLOP | время на M4 |
|---|---|---|---|---|
| рождение Embryo-0 | 55.9 M всего / 34.6 M активных (точный счёт по §3, `cortiq-embryo config`; из них 12.6 M — tied-эмбеддинг; в трейнере сейчас 27.8 M — без маршрутизируемых экспертов) | 500 M | 1.0·10¹⁷ | **измерено `step-bench`: 592 мс/шаг при B=8, T=1024 = 13.8k tok/s → ~10 ч** (иерархическая голова, hybrid_k на батч-GEMM) |
| рост ×10 шагов (новые слои/эксперты + маски) | +7 M за шаг | 100 M за шаг | 4·10¹⁵ | ~1 ч на шаг |
| навык (маска/эксперт по буферу OOD, 10–50 k примеров) | ~0.5 M | 5–20 M | | 5–20 мин |
| сон/простой (WTA-выделение, слияние, кристаллизация, генератор, реквант, гейты) | — | — | | 1–3 ч чистого простоя, набирается паузами |
| Embryo-2 суммарно | 400 M / 60 M акт. | ~5 B | ~2·10¹⁸ | ~3–4 недели фонового обучения |

Память: 60 M активных × 16 Б (веса+градиенты+Adam f32) ≈ 1 ГБ + активации при
seq 1024, batch 32 ≈ 3–4 ГБ. Влезает с большим запасом; замороженные части в bf16.

### 4.2 Данные (локально, без облака)
FineWeb-edu (выборка 1–2 B токенов, ~5 ГБ), FineWeb-2 ru (0.5 B), the-stack-smol
(0.3 B), math/GSM-стиль (0.1 B), диалоги (0.1 B, с контрактом «инструменты
только по объявлению»). Персональные данные — только в записи навыков
(никогда в ядро) — приватность и откат бесплатно.

### 4.3 Триггеры роста (все — из сигналов, которые уже есть)
- **навык**: буфер OOD-кластера ≥ N (например 200 входов) или плотный кластер;
- **эксперт**: маски навыков одного кластера дают < ε прироста при
  ненулевом резонансе (маска исчерпана) → новый эксперт копией ближайшего;
- **слой**: три ночи подряд суммарный прирост < ε при росте буфера
  («ёмкость исчерпана») → вставка тождественного слоя в место с максимальной
  стоимостью размещения P10 (там, где активации самые «загруженные»);
- **ширина**: морф P11 при исчерпании глубины (редко).
Каждый триггер оставляет след в журнале роста (запись в файле).

### 4.4 Гарантия «не забыть» (что именно гарантируется)
1. Байты старых записей неизменны (append-only) → любая старая задача
   воспроизводится побитово, если роутер выбирает ту же запись.
2. Роутер: новая запись добавляет дескриптор; старые дескрипторы
   неизменны; регресс маршрутизации измеряется на ночном гейте (матрица
   confusion навыков) — единственный канал регресса, и он измерим.
3. Кристаллизация ядра (P6) — единственное изменение ядра — под гейтом с
   симуляцией и откатом; версии генома хранятся.

## 5. Сон — алгоритм простоя

Сон запускается планировщиком простоя: `idle(N мин) ∧ util < 10% ∧ (питание
∨ батарея > 50%) ∧ температура в норме`. Шаги упорядочены по «ценность /
стоимость», каждый — атомарная запись; запрос пользователя прерывает шаг
(результат шага либо целиком дописан, либо отброшен), состояние цикла
сохраняется в журнале.

```
while idle():
    step = next_step_by_value()          # дешёвые и ценные первыми: prune_cold, merge_similar,
                                         # discover_skills(WTA), distill_system2, retrain_generator,
                                         # requant, reseal_anchors, crystallize_consensus, morph_or_retype
    candidate = step(state, budget=idle_slice)   # микро-шаг: секунды–минуты, чекпойнт внутри
    if interrupted(): rollback_step(); break     # запрос пришёл — уходим мгновенно
    if gate(candidate):        # ppl_ref ≤ 1.01·prev, каждая задача навыков ≥ prev−ε, needle 32k ≥ prev
        append(candidate)      # новая запись, старое не трогаем
    else:
        log_and_skip()
```
Гейт — тот же verdict-gate, что в `cortiq fcd`/`skill bake` (уже в CLI).
Полный цикл на M4 — 1–3 ч чистого простоя (§4.1), но он не обязан идти
подряд: сон набирается из пауз между запросами. На сервере с постоянной
нагрузкой сон — выделенная реплика или ночная квота; на ноутбуке — паузы
и подключение к питанию.

## 6. Что из этого патентуемо (сверх поданных P1/P2/P15/P16)

| # | предмет | новизна против прошлых наших и чужих | RTP |
|---|---|---|---|
| П-A **Developmental growth of a frozen-core network** | рост САМОГО ЯДРА (тождественно-инициализированные слои и эксперты) как append-only записей, с триггерами из резонанс-роутера (насыщение масок/ёмкости), размещение по P10-стоимости, гарантия byte-identity старого; чужие «progressive stacking / Net2Net» растут переписывая веса и без роутера/навыков | зародыш = первая реализация |
| П-B **Sleep consolidation cycle** | упорядоченный ночной цикл: WTA-выделение → дистилляция System2→1 → слияние → кристаллизация консенсуса → генератор масок → реквант → морф → reseal, каждый шаг под verdict-gate с откатом, всё append-only | зародыш |
| П-C **Фазово-когерентный O(1)-кор + двурежимный якорь o1-ready** | hybrid_k как обученный кор (не аппроксимация softmax) + якорь, у которого одни веса дают полный KV или O(1) (обучаемый reseal) — ворота PAM: подавать с датой нашего раскрытия фаз-оператора | оп в рантайме; обучение = зародыш |
| П-D **Recurrent-state speculative verify graph** | b-строчная верификация с рекуррентностью в регистрах без записи состояния, replay-коммит, zero-copy, shortlist-черновик | реализовано (0.5.82) |

Плюс зародыш — **reduced-to-practice embodiment для P3 и P4** (OOD→маска,
генератор G(z), консолидация; System 1/2 с дистилляцией) — это поднимает
их §112/§101-позицию и цену (в рейтинге P3 — коммерческий лидер портфеля).

## 7. Честные ограничения
- 20 M-зародыш — умный ребёнок, а не 27B; «вау» — в поведении системы
  (растёт, не забывает, спит, обменивается), а не в знании мира с рождения.
- hybrid_k к softmax при равном бюджете — **измерено двойниками на 56 M
  (23.08)**: гейт ≤ +10% ppl ПРОВАЛЕН с треском — гибрид 7:1 даёт val 71.06
  против 42.35 у полного softmax на тех же 61k шагах (+92%; на рантайме
  65.7 против 34.3, ×1.92). Разрыв виден уже на 5k шагах (99 против 254) и
  не закрывается — ранний сигнал зондов предсказателен. Вывод не «бросить
  O(1)-кор», а «одного якоря на 8 слоёв мало»: производственный ориентир —
  Qwen3.8-27B несёт 48 GDN : 16 softmax = 3:1. Дорожная карта качества —
  §10.
- Кристаллизация ядра и морф — самые рискованные шаги сна; поэтому они
  последними в очереди простоя и под самым жёстким гейтом.
- Стек — только Rust: наши Metal-ядра (`crates/cortiq-embryo`), никакого
  torch/MPS/Python; экспорт и загрузка — наш формат CMF. Длинные прогоны —
  шагами по процессу с чекпоинтом (пул Metal-буферов не держим часами).

## 8. План на 8 недель (всё на M4)
1. Нед. 1: тренировочный стек на Rust + наших Metal-ядрах
   (`crates/cortiq-embryo`, ни строки Python/torch). **Состояние 16.08:**
   - ядра: f32-GEMM на simdgroup-матрицах (TA/TB/beta, батч по (b,h,c),
     causal-маска в эпилоге), AdamW, RMSNorm, SwiGLU, softmax-CE, RoPE,
     causal-softmax fwd/bwd, gather/scatter; каждое — с паритетом против
     CPU-оракула f64 (`tests/kernels_parity.rs`); **2.7–2.8 TFLOPS**;
   - **hybrid_k обучаем**: CPU-оракул (рекуррентность + замкнутая форма
     backward, FD-gradcheck 1e-10) и Metal-чанк-скан C=64 (состояния по
     чанкам, интра/интер-чанк как батч-GEMM в лог-пространстве, обратный
     скан dS; GPU↔оракул 3e-7); 21 мс/слой при B=8;
   - граф Embryo-0 целиком (эмбеддинг → 7×hybrid_k + якорь GQA-softmax с
     RoPE → иерархическая голова 128×256 tied; flat-голова как двойник),
     арена p/g/m/v одним буфером; **gradcheck всего графа по каждому
     тензору** (`tests/model_gradcheck.rs`);
   - трейнер: шарды u16, сэмплер, warmup+cosine, чекпоинты с состоянием
     AdamW, `birth`/`step-bench`/профиль по фазам; дым: побайтовый корпус
     docs/*.md — Embryo-0 (vocab 256) loss 5.6 → 2.46, val ppl 30 → 18.6
     за 150 шагов;
   - **шаг Embryo-0 (B=8, T=1024): 592 мс = 13.8k tok/s** без экспертов
     (было 1378 мс до батч-GEMM hybrid_k и иерархической головы); с 4
     маршрутизируемыми экспертами — 890 мс холодный, ~1.2 с под
     троттлингом MacBook Air (без вентилятора) → 500 M токенов ≈ 20 ч.
   - **эксперты по резонансу (P1, без gate)**: дескриптор = μ_e (EMA
     входов) + зарезервированное 16-мерное подпространство; выбор argmin
     ошибки реконструкции − смещение (loss-free балансировка со шагом,
     масштабируемым средним резонансом); μ сеются строками первого батча
     (случайные центры коллапсировали в одного эксперта); слоты по
     детерминированному рангу с capacity 2×; per-expert GEMM с indirect
     dispatch — размер сетки и K берутся из счётчиков на GPU, readback нет.
   - **свой BPE-токенизатор 32k** (обучен за 72 с на смеси fineweb-edu /
     fineweb-2 ru / github-code / finemath), рантайм грузит его
     tokenizer.json и кодирует идентично (`tests/tokenizer_parity.rs`);
     шарды u16 (en 600 M, ru 300 M, code 100 M, math 100 M токенов),
     смесь по весам в `birth`.
   - **экспорт в CMF и паритет с рантаймом**: `cortiq-embryo export` пишет
     контейнер с именами тензоров загрузчика (`vmf_attn.*` с κ и A_log,
     `self_attn.*`, `mlp.shared_expert.*` + `mlp.experts.{e}.*` +
     `mlp.desc.*`, `lm_head.clusters.weight`, токенизатор в VOCAB); в
     рантайм добавлены `MoeConfig.router_resonance` (роутинг по
     дескрипторам) и `ModelArch.head_clusters` (двухуровневые log-prob);
     `tests/runtime_parity.rs`: **рантайм ↔ трейнер 1.9e-6 по log-prob,
     argmax совпадает**; `cortiq run` генерирует из экспортированного
     чекпоинта (CPU 277 tok/s).
   - **рождение Embryo-0 запущено 16.08 19:30** (61k шагов, B=8, T=1024,
     lr 6e-4 cosine, warmup 1000, wd 0.1; смесь en 0.6 / ru 0.2 / code 0.12
     / math 0.08; чекпоинт каждые 1000 шагов, лог `embryo-data/birth-e0.log`).
   - **навык за ночь (сцена 2) — механизм готов**: `cortiq-embryo
     skill-bake` (P2 DTG-MA + запись P15): маска нейронов над общим FFN
     выбранных слоёв (фаза A: L1 до дна денойзинга под held-out гейтом),
     FCD-полировка тех же FFN под жёсткой маской (фаза B, AdamW только по
     диапазонам навыка — остальная арена заморожена), маска свёрнута в
     replacement-тензоры, дескриптор роутинга P1 (φ после слоя, среднее +
     главные направления, как у `cortiq skill add`), запись `skill.{id}.*` +
     SkillRecord дописывается в CMF; `tests/skill_bake.rs` проверяет
     **байт-идентичность всех старых тензоров (хэши и payload)** и что
     рантайм грузит навык (`run --skill`, `skill list`, `route`). На
     геноме шага 1000 навык из docs/*.md собран за минуту.
   - **PCA-подпространства дескрипторов экспертов** (полный P1-резонанс):
     ковариация по слотам на GPU (dynamic-K GEMM), собственные векторы на
     CPU каждые 200 шагов → `desc.u`; рождение перезапущено с шага 1000 с
     этим включённым (чекпоинт с дескрипторами, resume).
   - рантайм: `cortiq ppl` (exact и `--o1 all`) работает на экспортированном
     геноме — O(1)-путь якоря есть без дообучения; на шаге 1000 o1-ppl даже
     ниже exact (недоученный якорь), финальную меру снимем после рождения.
   **Embryo-0 родился 17.08 14:47** — 61 000 шагов, 500 M токенов, 18 ч 41 мин
   на MacBook Air M4 (~1.1 с/шаг под троттлингом). Меры:
   - val ppl (смесь en/ru/code/math) **71.06**; в рантайме на held-out
     fineweb-edu (документы вне шардов) **65.7**; кривая ровная, эксперты
     сбалансированы (один «младший» в слое 0 — заметка для роста);
   - **гейт O(1) пройден без дообучения**: якорь в режиме sink 4 + окно 128
     + 32 лендмарка даёт ppl 70.18 против 69.79 exact на тех же токенах
     (`cortiq ppl --o1 all`), **×1.006**; с 16 лендмарками ×1.002 → сцена 4
     (длинный контекст при постоянной памяти) доступна через `--o1`;
   - речь: английский связный по грамматике, слабый по смыслу (уровень
     раннего GPT-2-small на 500 M токенах), русский слаб (20% данных — числа
     зацикливаются), код/математика вырождаются в повторы. Младенец, как и
     заявлено в §7: «вау» — в системе, знания придут ростом;
   - по дороге починен рантайм: батчевый путь `cortiq ppl` не применял
     коррекцию иерархической головы (плоские h·Eᵀ давали ppl 17697).
   - **сцена 2 на настоящем геноме**: навык `docs` из docs/*.md за 5 мин —
     маска нашла дно денойзинга (оставлено 53/53/72% нейронов слоёв 5–7,
     held-out улучшился ещё до полировки), после FCD ppl документов
     **303 → 232** (в рантайме 292 → 239); навык `code`: 34.7 → 29.9;
     **все базовые тензоры байт-в-байт неизменны** (224, потом 233 —
     второй навык не тронул первый); `cortiq route`: prompts про CMF → docs
     (E 0.16 vs 0.26), код → code (0.21 vs 0.27), посторонние темы — высокий
     E у обоих (0.34–0.49) = зона OOD (ядро без навыка).
   - **шаг сна «реквант под гейтом» существующим инструментом**: `cortiq requant
     --quant q4tp-quantize` (новый режим: настоящая квантизация float-контейнера)
     — файл 226 → 76 МБ (−66%), held-out ppl 65.7 → 70.0 (+6.5%), декод
     на CPU 305 → **400 tok/s**; гейт решает (проходит при пороге +10%,
     не проходит при +5%); 50 из 76 МБ — f32-эмбеддинг (q8 → ~35 МБ).
   - softmax-двойник (все 8 слоёв — якоря, тот же корпус и бюджет) запущен
     17.08 15:20, лог `embryo-data/birth-twin.log`, ~22 ч.
   **17.08 вечер — организм как процессы (§2, §5):**
   - **день**: `cortiq serve` с `CMF_OOD_DIR` трогает маркер простоя на каждый
     запрос и складывает OOD-запросы (по вердикту роутера навыков) в
     `buffer.jsonl`;
   - **роутер навыков = отлаженный рецепт cortiq-router** (`cortiq-bot/
     cortiq-router`): ошибка реконструкции на unit-φ, температура по NLL,
     ансамбль novelty (0.5·σ(z_top) + 0.25·margin + 0.25·(1−conf)), θ =
     (1−FPR)-квантиль in-scope; дескриптор записи несёт err_mean/err_std и
     held-out φ, файл перекалибровывает себя после каждого навыка
     (`CmfHeader.routing`); на рождённом геноме: CMF → docs, код → code,
     рецепт/фотосинтез → OOD, русский → docs (в корпусе docs есть README.ru);
   - **ночь**: `cortiq-embryo sleep` — при простое ≥ N мин и буфере ≥ M
     токенов печёт навык из буфера, гейт по held-out, проверка байт-
     идентичности всех базовых тензоров, атомарный коммит или откат,
     архив буфера, реквант под ppl-гейтом, перекалибровка роутера, журнал;
     **прерывается первым же запросом** (внутри шага обучения; буфер не
     тронут). Демо на рождённом геноме: ночь за 3.4 мин (ppl 45.8 → 39.3,
     реквант +6.5% ≤ 8% закоммичен), прерывание проверено;
   - **рост записями**: `grow` — E → E+1 экспертов в каждом слое (копия
     самого «горячего» по balancing-bias + сдвиг дескриптора вдоль главного
     направления; вставка тождественна), учатся только новые (остальная арена
     и старые дескрипторы заморожены), гейт по held-out; экспорт добавляет
     `mlp.experts.{E}.*` и per-expert дескрипторы — **ни один старый тензор
     не переписан** (48 байт-идентичных + 12 новых записей на tiny; рантайм
     грузит выросший геном без роутер-заглушки); в демоне — триггер после K
     отклонённых ночей подряд. На рождённом геноме первый рост held-out не
     улучшил → **отклонён гейтом** (механизм и гейт работают; когда рост
     окупается — вопрос данных/шага);
   - **MTP-головы** (t+2, t+3): RMSNorm + проекция через tied-голову на
     замороженном стволе, gradcheck 1e-4, `mtp-train` дописывает
     `model.mtp.{k}.*`; на 56 M ствол драфтит слабо (ppl 696/1114) — как и
     предсказано в §3.4; спекуляция в рантайме их пока не потребляет.
   - softmax-двойник в процессе: на шаге ~5000 val ppl 99 против 254 у
     hybrid_k на той же точке — softmax учится заметно быстрее на этой
     стадии; финальный гейт ≤ +10% ppl — по завершении (если провал: больше
     якорей и/или доработка кора; вывод пойдёт в §7 честно).
   Решения: o1-ready режим якоря НЕ дублируется в трейнере — после
   рождения экспортированный CMF идёт через существующие `convert --o1` +
   `fcd` (heal) рантайма (P16/P17); MTP-головы учатся поверх замороженного
   ствола после рождения (нужны спекуляции, не ядру); подпространство U
   дескрипторов (периодическая PCA) — следующий шаг, сейчас k-means-режим.
2. Нед. 2: **рождение Embryo-0** (500 M токенов) + softmax-двойник (гейт
   ≤ +10% ppl). Демо-сцена 1.
3. Нед. 3: роутер P1 + записи навыков + OOD-буфер + первый «навык за
   ночь» из пользовательских документов. Демо-сцена 2 (byte-identity).
4. Нед. 4–5: сон: WTA (P9), слияние, генератор (P3), реквант, гейты,
   откат. Демо-сцена 3.
5. Нед. 6: рост (эксперты, слои по триггерам), журнал роста; Embryo-1.
6. Нед. 7: длинный контекст (o1-режим якорей, 128k на маке), спекуляция.
   Демо-сцена 4.
7. Нед. 8: обмен навыками между двумя копиями (рой), отчёт с замерами;
   тексты П-A/П-B на данных.

*Ниже — прежний тир масштаба (20B-A2.4B под 24 ГБ мак) остаётся как
«взрослая» ветка того же генома; он не нужен для старта.*

## 9. Журнал рождений (измерено)

| геном | родился | архитектура | шаги/токены | val ppl | рантайм-ppl (heldout-en) | декод M4 |
|---|---|---|---|---|---|---|
| **Embryo-0** | 17.08 14:47, 18ч41м | 7×hybrid_k + 1 якорь GQA | 61k / 500M | 71.06 | 65.7 | ~400 tok/s (q4tp 76 МБ, +6.5% ppl) |
| **Близнец-softmax** | 23.08 07:38, 22.6ч | 8×softmax (`--anchor-every 1`) | 61k / 500M | **42.35** | **34.275** | 344.7 tok/s (f16 226 МБ) |
| **Embryo-1-conv** | 24.08 20:19 | Embryo-0 + свёртка k=4 перед миксером | 61k / 500M | 60.27 | **48.9** | 262.6 tok/s (f16 216 МБ, CPU) |

Оба — 55.8 M параметров (34.6 M активных), общий токенизатор/корпус/
гиперпараметры/сид; отличие ровно одно — миксер. Это и делает пару
измерением, а не впечатлением.

Что ещё подтверждено на паре:
- цепочка train → export → runtime сходится (`export` → `.cmf` → `cortiq
  run`/`ppl` без свap-слоёв и переконвертаций);
- роутинг экспертов на резонансе не коллапсирует за 61k шагов: все 4
  эксперта каждого слоя живы, в глубоких слоях парная специализация
  (`[3151, 1159, 539, 3343]`) — специализация, не вырождение;
- генерация 56 M-генома: связная грамматика, верная эпоха фактов,
  зацикливание содержания (норма ёмкости).

Ловушки, оплаченные временем:
- **мак спит — тренер спит**: ночное рождение замёрзло на 7 часов вместе
  с ноутбуком; длинные прогоны — только с `caffeinate -w <pid>`;
- ранний сигнал (val на 5–10k шагов) предсказывает финальный порядок
  архитектур — зонды дешевле полных рождений в 6 раз;
- пост-хок сжатие готовых весов общей факторизацией — НЕ путь консолидации
  сна: спектральная проба на 64 слоях Qwen3.8-27B дала почти плоский
  спектр (r8 = 16% энергии при равномерности 12.5%) — слои взаимно
  ортогональны, «общий кор + вектор на слой» из готового не извлекается.
  Консолидация — это обучение под структуру (как P2-bake), не SVD.

### 9.1 Семья зондов плотности/свёртки (23–24.08, все — 10k шагов,
одно расписание, один сид; val ppl @10k)

| вариант | якорей из 8 | conv | ppl @10k | к контролю |
|---|---|---|---|---|
| контроль | 1 | — | 177.8 | — |
| **короткая свёртка k=4** | 1 | ✓ | **165.4** | **−7.0%** |
| 2 якоря (слои 3,7) | 2 | — | ~166 (прерван на 8k: 176.7, экстрапол.) | ~−6.6% |
| близнец-референс | 8 | — | 74.5 (своё 61k-расписание) | −58% |
| conv + донор + дистилляция (v1) | 1 | ✓ | 170.2 | −4.3% (хуже чистой conv) |
| v2: + freeze-donor 1000 | 1 | ✓ | срезан на 3k: 396 (v1 там 356) | параллельно v1, −11% |
| **v3: conv + ТОЛЬКО дистилляция (холодный старт, w=1)** | 1 | ✓ | **~158 на 6.5k, идёт к 10k** | **ниже conv-кривой на 18–26% в каждой точке** |

Выводы, оплаченные ~15 часами зондов:
- **Свёртка бесплатна и равна второму якорю**: −7% за ноль времени шага
  и ноль потерь O(1) против −6.6% за софтмакс-слой с растущим KV. В
  финальное тело едет свёртка, якорь остаётся один.
- Подтверждён диагноз по позициям: плоский разрыв = ближнее смешивание
  (conv), не дальнее извлечение (якоря почти не двигают).
- Методическая ловушка на будущее: сравнивать зонды можно только на
  ОДНОМ расписании — 61k-кривая Embryo-0 на шаге 10k (169.8) на 4.5%
  «лучше» честного контроля (177.8) чисто из-за фазы LR.
- Остаток разрыва до близнеца (165 → 82) ложится на дистилляцию от него
  (§10 п.4) и дельта-правило (§10 п.5).
- **Тёплый старт v1 проиграл холодному** (25.08): init-from близнеца
  (143 тензора: embed/голова/нормы/FFN/эксперты + дескрипторы) +
  дистилляция по финальному hidden (поэлементный MSE, w=1) дали 170.2
  на 10k против 165.4 у чистой conv. Причина видна в кривой: на старте
  CE 11.97 — ХУЖЕ равномерного (10.4), настроенная голова уверенно
  ошибается на шуме свежих миксеров; к шагу 300 отставание 3×, дальше
  монотонное схождение (+21% на 2k → +3% на 9k) — донор
  «пересобирается» весь прогон и так и не окупается. dist-терм почти
  не движется (12.5→11.4 к шагу 300): у MSE по hidden высокий пол —
  O(1)-миксер не повторит softmax-hidden точно. Следующий атом: v2
  `--freeze-donor 1000` (прогрессивная замена: миксеры сначала учатся
  кормить замороженную сеть; wd на заморозке выключен — decoupled
  decay усыхал бы нулевой-градиентного донора на ~6%/1000 шагов).
- **v3: дистилляция подтверждена, трансплантация опровергнута** (25.08).
  Холодный студент + подтягивание финального hidden к близнецу (MSE,
  w=1) — первый зонд семьи НИЖЕ conv-кривой: 1k 447/602, 2k 306/387,
  3k 243/306, 5k 182/222; финальный conv-репер 165.4 побит уже на
  шаге 6000 (164.9). Учитель полезен «мыслями», не весами: перенос
  сигнала работает (−20%), перенос весов вредит (v1/v2). Это главный
  вердикт семьи — в тело Embryo-2 едет conv + дистилляция, без
  init-from.
- **v2 срезан на 3k**: после разморозки один крутой рывок (1680→599 за
  500 шагов) — и кривая ложится параллельно v1 с постоянными −11%
  (2k: 511/467, 2.5k: 445/401, 3k: 396/356). Замороженная тысяча —
  чистый убыток; ни заморозка, ни свобода не делают трансплантацию
  окупаемой. Направление init-from закрыто двумя зондами. Следующий
  атом v3: холодный старт + только дистилляция (w=1) — одна переменная
  против чистой conv (165.4); по |g| смоуков дистилляционный градиент
  при w=1 уже ~половина массы, «слабость» дистилляции тут ни при чём.
- **Полное рождение со свёрткой подтвердило зонд**: Embryo-1-conv на 61k
  дал val 60.27 против 71.06 у Embryo-0 (−15.2% — на длинном расписании
  свёртка платит вдвое больше, чем на 10k-зонде). Рантайм-ловушка: движок
  до 0.6.2 игнорировал экспортированный `vmf_attn.conv1d.weight`
  (проекции обучены на выходе свёртки → без неё ppl 93 539 и мусор);
  после фикса heldout-ppl **48.9** против 58.3 у Embryo-0 тем же бинарём,
  декод 262.6 tok/s на CPU M4 — O(1) сохранён (кольцо k−1 входов в
  хвосте состояния слоя, парный путь спекуляции тоже конволюционный).

## 10. Дорожная карта качества O(1)-ядра (решение 23.08: растим ветку
на наших технологиях — быстрее и O(1); разрыв +92% закрываем по шагам)

Порядок выбран ценой шага и тем, что каждый гейтуется числом:

1. **Диагноз по позициям** (день, только анализ): лоссы двойников по
   позициям и классам токенов. Разрыв к дальним позициям/повторам =
   голод извлечения (лечат якоря/окно/дельта-правило); ровный разрыв =
   голод ёмкости состояния (лечат state/conv). Выбирает ветку ниже.
2. **Плотность и размещение якорей** (зонды по ~4 ч до 10k шагов):
   `--anchor-every 2` и `4` (то есть 4 и 2 якоря из 8), якоря в
   середину-конец. Ориентир индустрии 3:1 (Qwen3.8). Гейт зонда: val@10k
   в +15–20% от близнеца@10k.
3. **Короткая свёртка перед миксером** (conv1d, 4 отвода — как у Qwen3.8
   и LFM2, у hybrid_k её нет): копеечный параметрами, снимает с
   рекуррентного состояния локальное смешивание. Вероятно, самый дешёвый
   крупный кусок разрыва.
4. **Дистилляция от близнеца** (✓ подтверждено зондом v3 25.08:
   −18…−26% к conv-кривой на всём расписании; финальный conv-репер
   побит на 60% дистанции). Форма — MSE по финальному hidden, w=1,
   холодный старт. Половина исходного пункта — инит от близнеца —
   ОПРОВЕРГНУТА двумя зондами (v1 свободный донор 170.2 против 165.4;
   v2 замороженный — параллельно v1 с −11%): веса не переносятся между
   бассейнами, переносится сигнал.
5. **Дельта-правило в миксере** (GDN-класс: стирание/замена в состоянии
   вместо чистого κ-затухания) — рантайм-ядра GDN уже есть, писать
   тренировочные (bwd на Metal). Отдельная кампания, если после 2–4
   разрыв ещё > +10%.
6. Дешёвая ёмкость по пути: state/головы линейной части ×2 (O(1)
   сохраняется); скользящее окно 128 на части слоёв (память константна —
   сцена 4 не страдает, извлечение локально точное).

Финальный гейт ветки прежний: val ≤ близнец +10% при ≥ половине слоёв
O(1), плюс проверка сцены 4 делом (длинный контекст: память и tok/s не
деградируют).

## 11. Перенос знаний крупных моделей (например Qwen3.8-27B) — оценка

Рамка из измеренного: веса не переносятся даже от идеального донора
(v1/v2 — близнец того же размера, того же токенизатора), сигнал
переносится отлично (v3). Разрыв ёмкости 27B→56M — ~500×: «все знания»
не влезут физически; влезают манера предсказывать и плотность полезного
текста. Отсюда три пути, по убыванию реализма:

1. **Синтетический корпус (данные-дистилляция)** — самый реальный.
   27B (у нас в формате CMF, гоняется нашим же движком) пишет студенту
   «учебник»: плотный текст/код/математику, продолжения нашего корпуса.
   Токенизация НАШИМ 32k-токенизатором — никакой стыковки словарей и
   размерностей. Зонд одной переменной: те же 10k шагов, часть корпуса
   заменена синтетикой. Комбинируется с v3-рецептом (hidden от
   близнеца + CE по синтетике).
2. **Логит-дистилляция от 27B** — вторым номером. Блокер — токенизатор:
   словарь Qwen ~150k против наших 32k; прямое сопоставление логитов
   невозможно, а эмбеддинг под 150k (58M параметров) больше самой
   модели. Возможный обход: снять top-k логиты учителя по корпусу один
   раз в сайдкар (~16 ГБ на 500M токенов, дни подового времени) и
   выравнивать по тексту приближённо — шумно, дорого, откладываем до
   пода и решения по токенизатору.
3. **Hidden-дистилляция через обучаемый проектор 384→hidden(27B)** —
   технически возможно, но известен эффект «слишком сильного учителя»:
   маленькому студенту полезнее учитель на расстоянии вытянутой руки
   (наш близнец) или его ДАННЫЕ, чем недостижимые внутренние
   представления гиганта.

## 12. Несколько зародышей: навыки, слияние, общение

Ключевой измеренный запрет: **сливать независимо рождённые зародыши по
весам нельзя** — v1/v2 показали это в миниатюре (даже одинаковая
архитектура и корпус, но другой оптимизационный бассейн → трансплантат
не приживается; усреднение весов чужих рождений — тем более). Что
РАБОТАЕТ и уже частично реализовано:

1. **Один ствол, навыки как записи** (реализовано: skill.rs + рантайм).
   Навык — это НЕ отдельный зародыш, а запись `skill.{id}.*` поверх
   общего генома: маска нейронов над общим FFN → FCD-полировка под
   маской → свёртка маски в тензоры → дескриптор маршрутизации (P1).
   Все старые тензоры бит-в-бит нетронуты (хэши доказывают), навыки
   накапливаются аддитивно, рантайм выбирает запись по дескриптору.
   «Несколько зародышей с разными навыками» на нашем стеке — это один
   геном со многими записями; слияние тривиально (конкатенация записей),
   потому что ссориться нечему.
2. **Рост экспертами** (реализовано: growth.rs). Хирургия E→E+1 в
   каждом слое: копия самого горячего эксперта + шум, дескриптор
   сдвинут вдоль первого направления подпространства — функция сети в
   момент вставки НЕ меняется, обучаются только новые эксперты, гейт по
   held-out против до-ростового генома. Это ось «зародыш растёт при
   накоплении»: новый домен → новые эксперты, роутер на резонансе сам
   учится, когда их звать.
3. **Слияние через дистилляцию (мульти-учитель)** — единственный
   надёжный способ слить РАЗНЫЕ рождения: свежий (или базовый) студент
   учится у N зародышей-специалистов, каждый преподаёт на своём домене
   (выбор учителя по домену примера). Опирается прямо на вердикт v3:
   переносим сигнал, не веса.
4. **Общение с обучением** — реалистичная форма ближнего срока:
   «детский сад» на проверяемых задачах. Зародыши крошечные и быстрые
   (сотни tok/s на CPU каждый), можно дёшево гонять диалоги, но чистое
   эхо друг друга деградирует — нужен арбитр. Арбитр без нейросети:
   задачи с проверяемым ответом (код исполняется, математика сходится);
   обмен фильтруется проверкой, победившие цепочки идут в корпус
   дообучения. Асимметричное общение (учитель→ученик) — это уже
   работающая дистилляция; симметричное без проверяемого фильтра —
   исследовательский риск, не план.
